// overlay on the mainScene with a text box on one side
// and repeating animation/gif/thing on the other

// click after instructions to start

import log from '../utils/logger'

export default class InstructScene extends Phaser.Scene {
  constructor() {
    super({ key: 'InstructScene' })
  }
}
